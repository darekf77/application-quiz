# Application quiz project with cloudflare backend

- basic http login token (@taon-dev/login)
- deployable to cloudflare backend and github page frontend (taon subproject)
- 2 vversions:
 + Normal (with simple http request)
 + NgRX (standard store ngrx action, selector etc.)
 + Singlar Store
